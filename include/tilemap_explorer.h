#pragma once

/// Compatibility forward: `TilemapExplorer` is now `using TilemapExplorer = Explorer<Tilemap>;`,
/// defined in `explorer.h` alongside the `TilemapLike` concept and the sparse sibling
/// `SparsemapExplorer = Explorer<Sparsemap>;`.
#include "explorer.h"
