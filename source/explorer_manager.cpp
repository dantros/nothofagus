// Template implementation lives in explorer_manager_impl.h so the explicit
// instantiations can sit in frame_runner.cpp next to the extern template
// declarations. This TU is kept (rather than removed) as a sanity check that
// the impl header compiles standalone.
#include "explorer_manager_impl.h"
